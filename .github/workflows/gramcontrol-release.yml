name: GramControl Release

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build_and_release:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: GramControl

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: npm
          cache-dependency-path: GramControl/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build & zip extensions
        run: npm run deploy

      - name: Read package version
        id: version
        run: |
          echo "value=$(node -e "console.log(require('./package.json').version)")" >> "$GITHUB_OUTPUT"

      - name: Create GitHub release and upload assets
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.version.outputs.value }}
          name: Cadence v${{ steps.version.outputs.value }}
          body: Auto-generated release
          files: |
            Cadence/dist/cadence-chrome.zip
            Cadence/dist/cadence-firefox.zip
            Cadence/dist/cadence-edge.zip